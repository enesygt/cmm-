#include <cmm/cmm.hpp>

int main(int argc, char *argv[]) {
    //cmm::config::parce_cmd_args(argc, argv);
    //auto& out = cmm::config::out_stream;
    //auto& in  = cmm::config::in_stream;
    //out << cmm::parce(in);
    //return 0;
}
